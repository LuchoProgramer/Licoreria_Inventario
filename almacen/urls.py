"""from django.urls import path
from . import views

urlpatterns = [
    path('', views.gestion_inventario, name='gestion_inventario'),            # Página principal de gestión de inventario
    path('agregar/', views.agregar_producto, name='agregar_producto'),        # Agregar productos al inventario
    path('actualizar/<int:id>/', views.actualizar_inventario, name='actualizar_inventario'), # Actualizar información del inventario
]"""
